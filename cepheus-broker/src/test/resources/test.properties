
# local broker base URL
local.url=http://localhost:8081

# remote broker base URL
remote.url=http://10.25.12.123:8081

# remote broker Service Name
remote.serviceName=gateway

# remote broker Service Path
remote.servicePath=gateway1

# remote broker OAuth token for secured brokers
remote.authToken=XXXXXXXXX

# forwading updateContext to remote broker
remote.forward.updateContext=true

spring.datasource.url=jdbc:sqlite::memory:
